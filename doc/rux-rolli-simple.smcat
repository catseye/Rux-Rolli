initial,
editing,
stopped,
terminated,
running;

initial => stopped;

stopped => editing : edit;
editing => stopped : done;

stopped => stopped : step;
stopped => running : run;
running => stopped : stop;
stopped => stopped : reset;

running => terminated : program
finished;
terminated => stopped : reset;
terminated => editing : edit;
